export type LoginValues = {
    username: string,
    password: string
}

export type RegisterValues = {
    username: string,
    password: string,
    email: string
}

export type RegisterResponseMessage = {
    message: string
}
